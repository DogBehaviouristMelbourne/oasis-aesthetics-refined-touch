// MCP remote server configuration and utility
// import { MCP_SERVER_URL } from './mcp.config';

// MCP utility not needed for now. (MCP integration paused)
// export async function mcpFetch(path: string, options?: RequestInit) {
//   const url = `${MCP_SERVER_URL}${path}`;
//   const response = await fetch(url, options);
//   if (!response.ok) {
//     throw new Error(`MCP request failed: ${response.status} ${response.statusText}`);
//   }
//   return response.json();
// }
